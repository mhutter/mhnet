---
all:
  vars:
    admin_user: mh
    admin_uid: 3388
    ssh_port: 6214

    ansible_user: "{{ admin_user }}"
    ansible_port: "{{ ssh_port }}"
  hosts:
    v2202607378119479704.powersrv.de:
      ansible_host: "2a03:4000:28:e79:6897:a4ff:fe0d:f8ad"

# Hosts placed here connect as root on port 22.
# Move a host out of this group once it has been bootstrapped.
bootstrap:
  vars:
    ansible_user: root
    ansible_port: 22
  hosts:
  #   some-host.example.com:
  #     ansible_host: "128.66.1.2"
